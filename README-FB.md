ไม่ใช่ครับ ❌
**`PAGE_TOKEN` ≠ App ID**
อันนี้สำคัญมาก เดี๋ยวสรุปให้ชัด ๆ แบบเทียบกันเลยครับ 👇

---

## สรุปสั้นมาก

| อะไร                               | ใช้ทำอะไร            | เอาไปใช้โพสต์ได้ไหม         |
| ---------------------------------- | -------------------- | --------------------------- |
| **App ID**                         | ระบุตัว Facebook App | ❌ ไม่ได้                    |
| **App Secret**                     | ลายเซ็นของ App       | ❌ ไม่ได้                    |
| **User Access Token**              | สิทธิ์ของ user       | ❌ (โพสต์ Page ไม่ได้โดยตรง) |
| **Page Access Token (PAGE_TOKEN)** | สิทธิ์ของ Page       | ✅ **ใช้อันนี้เท่านั้น**     |

---

## อธิบายทีละตัว (กันงง)

### 1️⃣ App ID คืออะไร

* เป็นแค่ **รหัสของแอป**
* หน้าตาประมาณนี้

```txt
123456789012345
```

❌ ใช้กับ Graph API เพื่อโพสต์ **ไม่ได้**

---

### 2️⃣ PAGE_TOKEN คืออะไร

* เป็น **Access Token ของ Facebook Page**
* เอาไว้โพสต์ / แก้ไข / อ่านข้อมูล Page
* หน้าตาประมาณนี้

```txt
EAAGm0PX4ZCpsBAJZCZA...ยาวมาก
```

✅ ใช้กับโค้ดนี้ของคุณได้เลย

```js
https://graph.facebook.com/v24.0/{pageId}/feed
```

---

## Flow ที่ถูกต้อง (ภาพรวม)

```text
User login
   ↓
User Access Token
   ↓
เรียก /me/accounts
   ↓
Page Access Token
   ↓
โพสต์ลง Page
```

---

## วิธีได้ PAGE_TOKEN แบบเร็ว (Dev/Test)

### 1️⃣ เข้า Graph API Explorer

👉 [https://developers.facebook.com/tools/explorer/](https://developers.facebook.com/tools/explorer/)

* เลือก App
* Get Token → **User Access Token**
* เลือก permission:

  * pages_manage_posts
  * pages_read_engagement
  * pages_show_list

---

### 2️⃣ เรียกดู Page + Token

```http
GET /me/accounts
```

ผลลัพธ์ตัวอย่าง:

```json
{
  "data": [
    {
      "name": "My Business Page",
      "id": "123456789012345",
      "access_token": "EAAGm0PX4ZCpsBA..."
    }
  ]
}
```

* `id` → `FB_PAGE_ID`
* `access_token` → `FB_PAGE_ACCESS_TOKEN` ✅

---

## ตัวอย่าง .env ที่ถูกต้อง

```env
FB_PAGE_ID=123456789012345
FB_PAGE_ACCESS_TOKEN=EAAGm0PX4ZCpsBA...
```

---

## เตือนแรง ๆ ⚠️

* ❌ อย่าใช้ App ID / App Secret ยิง API
* ❌ อย่าใช้ User Token โพสต์ Page
* ✅ ใช้ **Page Access Token เท่านั้น**
* 🔒 เก็บ token ใน `.env` เท่านั้น ห้าม push ขึ้น Git

---

ถ้าคุณอยากได้:

* ✅ **Long-lived Page Token (ไม่หมดอายุ)**
* ✅ Script ดึง token อัตโนมัติ
* ✅ Next.js API route + cron post
* ✅ โพสต์พร้อมรูป / link / scheduled post

บอกผมได้เลย เดี๋ยวจัดให้ตรงกับระบบที่คุณใช้อยู่ครับ 🔥
